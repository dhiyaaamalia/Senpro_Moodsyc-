import WebPlayback from "@/components/Playback";

export const metadata = {
  title: "Playback",
  description: "Playback page for users to play music.",
};

const PlaybackPage = () => {
  return <WebPlayback />;
};

export default PlaybackPage;
